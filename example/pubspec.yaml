name: primekit_example
description: Example app demonstrating Primekit modules.
publish_to: none
version: 1.0.0+1
resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  primekit:
    path: ../packages/primekit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

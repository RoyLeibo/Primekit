name: primekit_example
description: Example app demonstrating Primekit modules.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.41.3'

dependencies:
  flutter:
    sdk: flutter
  primekit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

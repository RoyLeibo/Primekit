/// Primekit Flags module.
///
/// Feature flags and remote config with typed values, TTL cache, A/B testing,
/// and pluggable backends (Firebase Remote Config, MongoDB Atlas, local).
///
/// ```dart
/// import 'package:primekit/flags.dart';
/// ```
library primekit_flags;

export 'src/flags/flags.dart';

# ads — Ad Integration

**Purpose:** Ad network integration (banner, interstitial, rewarded ads).

**Key exports:** See `lib/src/ads/` — explore `ads.dart` barrel for current API.

**Dependencies:** See `pubspec.yaml` for ad SDK packages.

**Maintenance:** Update this file with key classes once ads module is actively used in an app.

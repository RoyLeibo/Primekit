# contacts — Contact Picker

**Purpose:** Native contact picking. Simple module — 2 classes.

**Key exports:**
- `ContactsPicker` — opens native contact picker UI
- `PkContact` — value type (name, phone, email)

**Dependencies:** flutter_contacts

**Maintenance:** Update if contact field types change.

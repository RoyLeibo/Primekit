// Re-export the canonical FirebaseAuthInterceptor from the auth module.
// Both `package:primekit/firebase.dart` and `package:primekit/auth.dart`
// now export the same implementation.
export '../../../src/auth/firebase_auth_interceptor.dart';

/// Primekit Crash module.
///
/// Error boundaries, structured crash reporting with breadcrumbs,
/// and pluggable backends (Firebase Crashlytics, Sentry).
///
/// ```dart
/// import 'package:primekit/crash.dart';
/// ```
library primekit_crash;

export 'src/crash/crash.dart';

/// PrimeKit Calendar integration.
///
/// ```dart
/// import 'package:primekit/calendar.dart';
/// CalendarService.configure(MyCalendarProvider());
/// await CalendarService.instance.createEvent(event);
/// ```
library primekit_calendar;

export 'src/calendar/calendar.dart';

/// Primekit Sync module.
///
/// Offline-first repository with conflict resolution, sync state machine,
/// and pluggable backends (Firebase Firestore, MongoDB Atlas Data API, custom).
///
/// ```dart
/// import 'package:primekit/sync.dart';
/// ```
library primekit_sync;

export 'src/sync/sync.dart';
